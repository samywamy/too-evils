<div class="container">
<h1>Upvote</h1>
<p>Vote questions into the database. Each question requires <%= @max_votes %> votes to go through.</p>

<table class="table table-hover">

    <tr><%= react_component("Upvote", { votees: @qs, max_votes: @max_votes }) %> </tr>

</table>

</div>